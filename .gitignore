node_modules/
.next/
out/
build/

# Secrets — never commit. .env.local.example is the checked-in template.
.env
.env.local
.env*.local

*.tsbuildinfo
next-env.d.ts

.DS_Store
*.pem
npm-debug.log*
yarn-error.log*

.vercel

# Local agent/editor tooling — launch.json just points Claude Code at
# `npm run dev` on port 3000, which anyone can configure themselves.
.claude/
