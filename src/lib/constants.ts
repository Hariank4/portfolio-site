// Feeds canonical URLs, Open Graph tags, JSON-LD and sitemap.xml — so this
// must be the domain the site is actually served from, not the .vercel.app
// preview URL. Update it here and everywhere else follows.
export const SITE_URL = "https://hariankjuneja.tech";
